[project]
name = "djhotel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.4",
]

[dependency-groups]
dev = [
    "django-extensions>=4.1",
    "ipython>=9.12.0",
]
